# Vincent 输入法：iQOO Neo8 测试说明

适用设备：iQOO Neo8、Android 16、OriginOS 6。

这是第一版个人测试构建，只包含 `arm64-v8a`，主输入法和 Rime 插件需要一起安装。安装包没有压缩，不会修改电脑上的小狼毫。

## 安装顺序

1. 先安装主应用 APK：`app/build/outputs/apk/debug/` 目录内的 APK。
2. 再安装 Rime 插件 APK：`plugin/rime/build/outputs/apk/debug/` 目录内的 APK。
3. 打开“Vincent 输入法（iQOO 测试）”，按照引导启用输入法。
4. 在系统输入法选择器中切换到 Vincent 输入法。
5. 如果第一次没有出现中文候选，关闭输入法设置页后重新打开一次；仍未出现时，重启主应用。

手机提示“未知来源应用”时，只给当前文件管理器临时安装权限，安装完成后可以立即关闭该权限。

## 第一轮测试

请在普通记事应用中依次测试：

- 输入 `wo jintian chi fan`，应能得到“我今天吃饭”。
- 输入 `github`，候选中应出现 `GitHub`。
- 输入 `comfyui`，候选中应出现 `ComfyUI`。
- 输入 `ue5`，候选中应出现 `Unreal Engine 5` 或 `UE5`。
- 中英文切换、退格、回车、数字和符号应正常工作。

## 剪贴板隐私默认值

本构建默认：

- 开启普通剪贴板历史，复制后可以直接在键盘剪贴板面板中使用；
- 不显示剪贴板建议；
- 普通剪贴板历史上限为 `300` 条；
- Android 标记为“敏感”的剪贴板内容只保留在运行内存，不写入本地数据库；
- 禁止系统云备份和设备迁移备份输入法数据；
- 主输入法 APK 不申请联网权限。

普通历史保存在输入法自己的应用沙箱中，供日常非敏感复制使用，但不采用保险箱的 AES 加密。密码、验证码、私钥、助记词或身份证信息应使用“安全剪贴板”，不要保存在普通历史中。普通历史可以随时在设置中关闭或清空。

输入法无法阻止“当前正在输入的应用”读取你输入到它自己文本框里的内容；系统级、Root 或高权限无障碍软件也不在普通输入法的控制范围内。

## 安全剪贴板保险箱

保险箱仅支持纯文本，使用 Android Keystore 中不可导出的 AES-256-GCM 密钥加密。数据库只保存密文、随机 IV 和经过认证的兼容性元数据，不保存明文标题、预览或字符数量。

最安全的保存方式：

1. 打开键盘的“文本编辑”。
2. 选择要保存的文字。
3. 点击顶部的锁形按钮“安全复制”。
4. 文字会直接进入加密保险箱，不经过系统剪贴板。

保存已经复制的内容：

1. 打开普通剪贴板面板，点击顶部锁形按钮进入“安全剪贴板”。
2. 点击保存按钮，将当前系统剪贴板加密。
3. 加密成功后，输入法会立即清空系统剪贴板，并移除普通历史中的同内容副本。

保险箱条目不会按时间自动过期，也不会在粘贴后自动删除。打开保险箱时，输入法会在内存中解密并显示内容；点击条目后由输入法直接写入当前文本框，明文不会重新放回系统剪贴板。长按条目可以手动删除。

显示保险箱时会禁止系统截图和录屏，并阻止无障碍服务读取条目视图。解密后的文字为了显示和粘贴会短暂存在于输入法进程内存中；Root、系统级恶意软件以及当前接收输入的应用不在普通输入法能够完全防护的范围内。

普通复制再导入保险箱仍存在“复制完成到加密清空之间”的短暂明文窗口。真正敏感的内容优先使用“选择文字 → 安全复制”，并同时开启 OriginOS 6 的剪贴板隐私保护。

## 剪贴板测试

1. 复制一段普通测试文字，打开键盘的剪贴板面板，确认它直接出现在普通历史中。
2. 连续复制多段普通文字，确认可以点击历史条目直接粘贴。
3. 打开“文本编辑”，选择一段敏感测试文字并点击锁形按钮，确认它只出现在安全剪贴板。
4. 点击安全条目完成粘贴，再次进入安全剪贴板，确认该条目已经删除。

## 卸载与恢复

在系统应用管理中分别卸载：

1. “Vincent Rime 插件（测试）”
2. “Vincent 输入法（iQOO 测试）”

卸载后切换回系统输入法即可。本测试构建和电脑上的 Windows 小狼毫互不影响。
